%ЛР_2, Задание 4, Вариант 4
%В бутылке, стакане, кувшине и банке находятся молоко, лимонад, квас
%и вода. Известно, что вода и молоко не в бутылке, сосуд с лимонадом находится между
%кувшином и сосудом с квасом, в банке - не лимонад и не вода. Стакан находится около
%банки и сосуда с молоком. Как распределены эти жидкости по сосудам.

%Есть ли элемент B в списке A
in_list(X, Y):- member(Y, X).

main:-
    %Объявляем список из 4-х элементов
    Liq = [_, _, _, _],

    %Объявляем правила для проверки каждого из первых 3-х элементов, которые соответсвуют одному из значений
    in_list(Liq, [water, Liq_1]),
    in_list(Liq, [milk, Liq_2]),
    in_list(Liq, [lemonade, Liq_3]),
    in_list(Liq, [kvass, Liq_4]),

    %Правила для проверки последних элементов списка
    in_list(Liq, [_, bottle]),
    in_list(Liq, [_, glass]),
    in_list(Liq, [_, jug]),
    in_list(Liq, [_, pot]),

    %Исключение значения bottle для 1-го и 2-го элементов
    Liq_1 \= bottle,
    Liq_2 \= bottle,

    %Исключение значения pot для 3-го и 1-го элементов
    Liq_3 \= pot,
    Liq_1 \= pot,

    %Вывод получившегося списка
    write('Water - '), write(Liq_1), nl,
    write('Milk - '), write(Liq_2), nl,
    write('Lemonade - '), write(Liq_3), nl,
    write('Kvass - '), write(Liq_4), nl.
